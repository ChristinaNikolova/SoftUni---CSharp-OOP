using BorderControl.Contracts;
using BorderControl.Models;
using System;
using System.Collections.Generic;
using System.Text;

namespace BorderControl.Factories
{
    public static class IdentifiableFactory
    {
        //public static IIdentifiable Create(string[] elements)
        //{
        //    IIdentifiable identifiable = null;

        //    if (elements.Length == 3)
        //    {
        //        string name = elements[0];
        //        int age = int.Parse(elements[1]);
        //        string id = elements[2];

        //        identifiable = new Citizen(name, age, id);
        //    }
        //    else if (elements.Length == 2)
        //    {
        //        string model = elements[0];
        //        string id = elements[1];

        //        identifiable = new Robot(model, id);
        //    }

        //    return identifiable;
        //}
    }
}
